{
  "schema_version": 1,
  "source_roots": ["src/rc_metastudio", "scripts", "r/RCMetaR/R"],
  "python_suffixes": [".py"],
  "r_suffixes": [".R", ".r"],
  "exclude_globs": [
    "*/forms/ui_*.py",
    "*/forms/*_rc.py",
    "*/__pycache__/*",
    "build/*",
    ".venv/*"
  ],
  "forbidden_imports": {
    "src/rc_metastudio/project_*.py": ["PyQt6", "rpy2"],
    "src/rc_metastudio/*domain*.py": ["PyQt6", "rpy2"],
    "src/rc_metastudio/workspace_*.py": ["PyQt6", "rpy2"]
  },
  "complexity_exceptions": {
    "scripts/code_health.py": {
      "tracked_paths": "walks Git's NUL-delimited inventory and applies independent path predicates",
      "python_metrics": "combines two analyzers while preserving per-function identity",
      "history_for_path": "parses Git's commit/numstat stream across three time windows",
      "python_import_graph": "boundary parser must classify imports, relative modules, and policy violations",
      "grimp_evidence": "revision snapshots require an isolated archived package for the locked analyzer",
      "gate": "combines independent cycle, import, and changed-function gates",
      "build_evidence": "assembles the versioned evidence document from independent measurements"
    },
    "scripts/build_macos_direct_provenance.py": {
      "main": "release provenance checks must retain independent tool, digest, and target diagnostics"
    },
    "scripts/delivery.py": {
      "verify": "validates every immutable release stage and its digest chain"
    },
    "scripts/native_analysis_smoke.py": {
      "main": "ships one end-to-end packaged analysis scenario"
    },
    "scripts/native_calculator_smoke.py": {
      "_run_main": "drives the packaged calculator through its required UI and backend lifecycle"
    },
    "scripts/native_remaining_surfaces_smoke.py": {
      "_capture_surface": "captures each independent packaged surface with fail-closed diagnostics"
    },
    "scripts/native_results_smoke.py": {
      "validate_evidence": "validates the complete packaged results evidence contract",
      "_run_scale": "runs each supported packaged scale with isolated evidence"
    },
    "scripts/r_dependency_policy.py": {
      "load_policy": "normalizes the two-target dependency manifest and validates all required fields"
    },
    "scripts/resolve_macos_package_target.py": {
      "load_target": "validates the single supported native target and its dependency record"
    },
    "src/rc_metastudio/analysis_dataset.py": {
      "cmp_studies": "orders heterogeneous study rows using the released comparison contract"
    },
    "src/rc_metastudio/analysis_unit.py": {
      "__init__": "constructs one validated domain unit for all supported data families"
    },
    "src/rc_metastudio/project_adapter.py": {
      "project_to_dataset": "decodes and validates every released dataset family before live mutation"
    },
    "src/rc_metastudio/publication_bias.py": {
      "__post_init__": "validates the immutable small-study request's cross-field contract"
    },
    "src/rc_metastudio/automation.py": {
      "start_automation_smoke": "hidden packaged qualification hook coordinates startup and teardown at one boundary",
      "dispatch": "the hidden packaged hook has one explicit command boundary for shell and project smoke modes"
    },
    "src/rc_metastudio/continuous_data_dialog.py": {
      "val_changed": "Qt table validation dispatches family-specific fields and feedback",
      "_cell_data_not_valid": "one validation boundary maps the supported continuous input fields to their distinct user-facing errors",
      "impute_pre_post_data": "pre/post imputation preserves the authority package's distinct input paths",
      "_restore_dialog_state": "adapter restores an isolated dialog copy and its visible controls",
      "_update_back_calculation_button": "calculator coordinates authority imputation, validation, and visible state",
      "clear_form": "clearing a calculator preserves each supported effect and imputation field contract"
    },
    "src/rc_metastudio/plot_capabilities.py": {
      "_validate_descriptor": "boundary validation checks independent capability fields before immutable construction"
    },
    "src/rc_metastudio/dataset_table_view.py": {
      "paste_contents": "Qt clipboard parsing coordinates a rectangular edit across visible adapter cells",
      "_preflight_paste": "clipboard validation checks rectangular shape, bounds, and every candidate cell before mutation",
      "row_header_clicked": "the Qt adapter selects one isolated family editor and publishes its accepted snapshot",
      "keyPressEvent": "Qt keyboard dispatch preserves the table's standard navigation and clipboard commands"
    },
    "src/rc_metastudio/binary_data_dialog.py": {
      "update_back_calculation_button": "binary calculator coordinates authority imputation, validation, and visible state",
      "val_changed": "Qt table validation dispatches raw-count and effect fields with family-specific feedback"
    },
    "src/rc_metastudio/diagnostic_data_dialog.py": {
      "val_changed": "Qt diagnostic validation dispatches raw-count and effect fields with family-specific feedback",
      "update_back_calculation_button": "diagnostic calculator coordinates authority imputation, validation, and visible state"
    },
    "src/rc_metastudio/main_window.py": {
      "add_new": "the three dimension-specific editors have intentionally distinct validation and selection transitions",
      "open": "interface boundary coordinates prompt, decode, replacement, and recent-project updates",
      "save": "interface boundary coordinates save-as prompts, serialization, and recent-project updates"
    },
    "src/rc_metastudio/csv_import.py": {
      "parse_csv": "the CSV boundary coordinates dialect handling, strict schema validation, row normalization, and typed result construction"
    },
    "src/rc_metastudio/main_wizard.py": {
      "_rebuild_display": "the Qt import-preview boundary coordinates cached-state reset, parser feedback, and visible table updates"
    },
    "src/rc_metastudio/plot_service.py": {
      "_apply_standard_edits": "the transaction boundary stages R parameters and artifacts before atomic promotion with rollback"
    },
    "src/rc_metastudio/qt_workspace_columns.py": {
      "fit_available_width": "the Qt layout adapter reconciles persisted user widths, automatic columns, and current viewport geometry"
    },
    "src/rc_metastudio/adaptive_window.py": {
      "eventFilter": "Qt event filtering preserves the adaptive window lifecycle across native resize and teardown events"
    },
    "src/rc_metastudio/r_bridge.py": {
      "_r_param_value": "the R boundary preserves nested vectors and recursively validates foreign parameter values"
    },
    "r/RCMetaR/R/meta_methods.R": {
      "bootstrap": "statistical resampling has independent method, seed, and result-shape branches"
    },
    "r/RCMetaR/R/plotting.R": {
      "create.subgroup.plot.data.generic": "plot data must preserve the package's independent subgroup layouts"
    },
    "r/RCMetaR/R/rcmetar-core.R": {
      "rcmetar.validate.analysis.request": "the public boundary validates independent request fields before dispatch"
    },
    "r/RCMetaR/R/reitsma_methods.R": {
      "rcmetar.reitsma.draw": "the authority renderer handles device, geometry, and output-format branches",
      "rcmetar.draw.reitsma.coefficient": "the authority renderer handles coefficient plot layouts"
    },
    "r/RCMetaR/R/results_display.R": {
      "create.regression.display": "the display contract preserves independent coefficient and fit summary sections"
    }
  },
  "gates": {
    "max_changed_cyclomatic": 15,
    "max_changed_cognitive": 20,
    "max_changed_nesting": 4,
    "block_new_cycles": true,
    "block_forbidden_imports": true
  }
}
